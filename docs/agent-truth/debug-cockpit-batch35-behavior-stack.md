# Debug Cockpit Batch 35 Behavior Stack

Batch 35 repairs behavioral, task, telemetry, recovery, and experiment debug source contracts so loaded all-zero shells cannot masquerade as truth.

Before:
- Behavior normalization, task catalog, runtime sample, task telemetry mapping, telemetry coverage, behavioral intelligence, telemetry truth recovery, and experiment registry could show loaded zero values without source-window, formula, rebuild, or registry status.

After:
- Zero rows route through the shared source-window classifier.
- Behavior health exposes formula/numerator/denominator/confidence state.
- Task catalog/runtime lanes expose catalog and sample source state.
- Task telemetry mapping flags catalog/mapping contradictions and alias scan status.
- Behavioral intelligence exposes deterministic baseline and rebuild provenance.
- Telemetry truth recovery exposes formulas and quality state.
- Experiment registry distinguishes missing registry from intentionally empty registry.

Remaining gaps are data/source/formula gaps, not fabricated passes.
