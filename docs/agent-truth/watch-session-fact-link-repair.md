# Watch Session Fact Link Repair

Watch sessions and session facts must link by `watchSessionId` or viewer-session identity. Missing links are classified as `watch_session_fact_link_missing`.

Replay-recovered sessions stay degraded and are not promoted to exact captures without evidence.
