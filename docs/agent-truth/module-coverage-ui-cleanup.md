# Module Coverage UI Cleanup

The Advanced Debug module coverage panel separates Required modules from Optional modules. Each module row shows accepted sources, canonical gaps, external optional gaps, dependent panels, next action, and validator ownership.

Raw technical evidence remains collapsed by default. GA4 is only shown as an external optional gap for modules where policy marks it optional.
