# Machine Learning Interaction Tracking Schema
# Stores AI prompt/response pairs for analytics and future model fine-tuning

type AiInteraction @table(name: "ai_interactions", singular: "aiInteraction", plural: "aiInteractions") {
  id: UUID! @default(expr: "uuidV4()")
  userId: String!
  modelUsed: String!
  promptContent: String!
  responseContent: String!
  promptTokens: Int
  completionTokens: Int
  latencyMs: Int!
  createdAt: Date! @default(expr: "request.time")
}
