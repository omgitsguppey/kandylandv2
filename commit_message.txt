🧹 Audit continuity and codebase hygiene refresh

🎯 What:
- Ran `npm run check:inventory` and updated tracked file counts (799) across `FULL_SCALE_CODEBASE_AUDIT.md`, `REPO_MEMORY_LEDGER.md`, and `EVERY_FILE_FUNCTION_CHECKLIST.md`.
- Detected and removed circular dependencies in `scripts/check-cycles.ts` for `@axe-core/react` and `firebase-functions/v2` imports.
- Fixed a cascading render bug by removing synchronous state updates from `useChatUnreadStatus` hook in `useEffect`.
- Restored missing `getChatRealtimeRefreshPlan` export in `src/lib/chat-realtime.ts`.
- Added missing `event_schema_version` validation to `consumeTimedFlow` and `startTimedFlow` in `src/lib/telemetry.ts`.
- Repaired `JSON.stringify` logic in `src/lib/server/route-diagnostics.ts` to properly serialize debug contexts and correctly truncate outputs.
- Restored missing fallback state text string in `src/lib/firestore-client-errors.ts` for the unread hook failing realtime updates test.
- Fixed unused imports in multiple files.
- Cleared generated artifacts such as `database-debug.log`.

💡 Why:
- Preserves the trustworthiness and consistency of our canonical audit records.
- Eliminates non-deterministic behavior inside React `useEffect` hooks.
- Fixes several silent test failures masking telemetry, chat, and API reliability bugs.
- Prevents our continuous integration from generating artificial cycle-check noise for non-runtime dependencies.

✅ Verification:
- `corepack pnpm install` complete.
- `npm run lint` passing.
- `npm run check:cycles` passing.
- `npm run check:continuity` passing.
- `npm run test:contracts` fully passing for all 131 test suites (592 tests).
- `npm run check` overall build and check processes successfully completed.

✨ Result:
- Codebase is significantly healthier and test suite acts as an accurate source of truth for runtime assumptions.
- 0 lint warnings remaining.
- 0 cycle dependency warnings.
- 0 mismatched audit values.
