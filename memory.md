## Current Operating Memory: KandyDrops Phase 1

- Use direct-fix mode when source identifies the issue.
- No backend-only or UI-only fixes; close the source-to-UI loop.
- Trace the canonical source first; do not add parallel metrics, events, or snapshots to avoid fixing the canonical path.
- Paid bundle bonus GumDrops are paid/purchased bonus credits, not reward/free GumDrops.
- Reward/free GumDrops cannot be used for creator experiences.
- Creator experiences must spend purchased GumDrops only and record creator attribution.
- Booking Phase 2 must not run unless GumDrop economy Phase 1 is committed on main.
- Creator booking UX should use generated available slots, not arbitrary fan-selected date/time.
- Creator owners should not see fan purchase, request, booking, or chat controls on their own profile.
- Creator management surfaces should show own creator drops only; public discovery stays public.
- Release-note-only commits must use `[skip release-notes]`.
- Public Beta notes track accepted public beta releases, not raw commits; a focused source/config/UI patch does not automatically update the Beta badge unless accepted into the public release-note bundle.
- Beta badge / patch notes should ship with the accepted release bundle or accepted patch slice. Release-note automation validates only and must not create separate follow-up loop commits.
- Cloud Run, Cloud SQL, Gemini/Cloud Assist, and 4xx cost checks are evidence/inventory lanes unless source code proves active usage or P0/P1 risk.
- Do not touch admin backend during user/creator UI parity work unless explicitly scoped.
- External audits are planning input, not local proof. Convert each finding into a bounded source slice before coding.
- The June 2026 GPT source audit did not run local tests or verify runtime, so its commit/score observations are hypotheses only. Accept its consolidation direction, not its numbers as proof.
- Browser smoke, screenshots, and test-account sessions are optional diagnostics for local UI/admin readiness, not beta-exit authority or source-readiness blockers.
- If a gate asks for manual screenshot proof, fix the source/evidence boundary so route contracts, selectors, hydration markers, client-error fixtures, and source reports identify the issue first.
- Local workflow notes must route routine UI/admin verification through source coverage and runtime contracts first; `check:ui:audits` is promoted browser reproduction only when doctrine, selector output, a source finding, or the operator explicitly asks for it.
- If generated task contexts, selectors, package-script descriptions, or older validators still mark browser smoke, Playwright, Lighthouse, screenshots, or authenticated test sessions as default-required, treat that as stale tooling debt. Current operator doctrine wins until the selector/registry is corrected.
- Do not churn historical/generated report docs just to rewrite stale screenshot/browser wording. Current operator doctrine overrides stale report snapshots; refresh, retire, or rewrite a report only through its owning validator/report lane.
- Replace generic manual-proof counters with typed states such as source_missing, bridge_missing, materializer_missing, external_proof_required, runtime_proof_required, or admin_truth_required.
- Admin truth samples must be structured redacted artifacts first: JSON/source sample, route/report key, freshness timestamp, sample count, and source-state labels. Screenshots are optional visual context only and cannot clear admin truth by themselves.
- Consolidation is deletion-first but not reckless: retire docs, reports, scripts, panels, or validators only after consumer searches prove they are duplicate, superseded, archive-only, or disconnected.
- Do not replace the active doctrine hierarchy with a new agent/objectives stack unless a validator-backed migration updates consumers and retires superseded lanes in the same slice.
- Analytics evidence uses the local vocabulary observed, modeled, inferred, cached, missing, privacy_limited, late_arriving, and external_proof_required. Treat modelled as a legacy/external alias only.
- Official Google/YouTube analytics docs are implementation references only. GA4 modeled/late attribution and YouTube visible-impression rules must be mapped into KandyDrops source states, confidence, dedupe, and product-truth boundaries before use.
- External-audit reduction targets are cleanup pressure, not authority; docs, scripts, browser dependence, components, generated reports, and proof gates require local consumer proof before deletion, retirement, or relabeling.
- Do not answer audit-driven sprawl by adding another validator, generated report family, objective stack, doctrine family, or dashboard panel unless the same source slice retires or replaces the duplicate owner.
- Package-script and validator cleanup must route through an existing registry, selector, or canonical owner where possible. A human alias can stay, but independent report truth and stale generated outputs should retire with the superseded lane.
- Control Tower is still the gateway for UI/copy/state/telemetry/admin truth, but it is compact-context first. Do not use old fixed-read-order language to open every doctrine file for narrow work.
- Root `AGENTS.md` truth order now puts `docs/agent-truth/current-operator-doctrine.md` ahead of broad historical ledgers for Phase 1 operator workflow, source-first proof, and external-audit intake.
- Current audit-derived coding slices are source-first admin smoke, creator/admin Drop creation validation clarity, admin create/drop AI panel removal, action-first Admin AI/debug workbench, and telemetry/analytics consolidation through existing owners.
- Execute those audit-derived slices separately. Each slice must name the canonical owner, prove consumers/imports before retirement, keep formal runtime/provider/admin evidence gates intact, and end with a duplicate/stale-lane grep.
- External-audit commit names, score values, script lists, and UI-state claims must be rechecked against local source, `git log`, package scripts, generated artifacts, and owning validators before being used as repo truth.
- Do not create `agent/objectives/*`, broad new validators, new report families, or replacement doctrine stacks from external-audit advice unless the same focused migration updates consumers and retires old lanes.
- Doctrine consultation itself is compact-first now. `.agent/skills/doctrine-consultation.md` and `.agent/workflows/ui-copy-refinement-workflow.md` must route through generated task context, current operator doctrine, surface doctrine, source owners, telemetry/admin/debug paths, and source validators before browser reproduction. Do not use stale `view_file` instructions or old broad doctrine reads as the default path.
- Public beta score/readiness doctrine must not use loose visual/manual evidence as a default gate. Browser or visual diagnostics are optional reproduction unless a current formal runtime contract promotes them; provider/runtime/admin proof must be typed, schema-backed external evidence.
- Pre-commit and coding startup are compact-first. Open the large audit/memory/checklist ledgers only when the generated task context marks broad startup as required, governance/tooling/shared helpers change, or source ownership remains unresolved.
- Durable memory writeback is not required for every broad or ordinary source fix. Update `FULL_SCALE_CODEBASE_AUDIT.md`, `REPO_MEMORY_LEDGER.md`, or `EVERY_FILE_FUNCTION_CHECKLIST.md` only for doctrine/governance changes, owner or validator promotion/retirement, verification-policy changes, or validator-required memory writeback.
- Lighthouse, Playwright, Cypress, and browser-smoke commands are promoted diagnostics only. Even performance-sensitive UI work should start from deterministic source/device/layout/hydration contracts unless selector policy, current doctrine, a concrete source finding, or the operator explicitly requests browser reproduction.
- Local admin fixtures, selector probes, route contracts, hydration markers, generated source reports, and client-error fixtures are source-readiness evidence only. They do not become deployed runtime smoke, provider smoke, payment proof, admin truth sample proof, or production operator proof unless a separate formal artifact contract says so.
- Audit-driven cleanup should make the repo feel like one coordinated body: compact context for startup, current operator doctrine for posture, surface doctrine for conflict winners, canonical source modules for runtime truth, generated reports as snapshots, and formal artifacts for runtime/provider/admin proof. Do not create another objective stack, validator family, or dashboard panel to reconcile lanes that should be merged, retired, or demoted.
- Convert the June 2026 GPT audit into separate issue slices before coding: source-first admin smoke boundary, creator/admin Drop collapsed-section validation clarity, ordinary admin create/drop AI panel removal or diagnostics-only ownership, action-first Admin AI/debug UI, package-script/report consolidation by consumer proof, and telemetry/analytics launch recovery through existing catalog/translation/identity/recovery/materializer owners.
- Workflow files and generated command lists are menus, not automatic authority. GitHub PR commands, `git push`, browser audits, Lighthouse, Firebase rules checks, provider checks, deploy-adjacent checks, and full `npm run check` require explicit scope, selector promotion, release-risk scope, or a formal evidence contract. Missing external command output should be classified as optional diagnostic or external_evidence_required, not local source failure.

## Current Operating Memory: KandyDrops Phase 2

- Creator booking UX uses generated availability slots, not arbitrary fan date/time.
- Creator owners viewing their own public profile do not see fan purchase/request/booking/chat controls.
- Creator management/drop surfaces show own creator drops; public discovery remains public.
- Phase 2 depends on Phase 1 GumDrop economy contract and must not change paid-bonus ledger math.
- Bug report rewards credit reward GumDrops only, never purchased balance.
- User and creator UI must use HumanErrorNotice or equivalent translated errors; raw dev errors belong in Debug/admin evidence.
- Normal UI shows translated errors; Debug/admin shows raw/structured error truth. Bug report rewards are reward GumDrops only.
